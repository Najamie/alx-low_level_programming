#include "main.h"
/**
 * mul - Multiplies two integers
 * @a: The digit to be multiplied to b
 * @b: The digit to be multiplied to a
 * Return: Always 0
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
